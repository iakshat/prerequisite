Hey this is authentication using passwords saved with hashing 
Requirements:

use npm install from the terminal first
then get a mysql installed for terminal
Change the username password in server.js as per your system
Now run tge server using node server.js
