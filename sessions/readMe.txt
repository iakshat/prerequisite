Login system using sessions in server with sessions getting stored in DB for no data loss if server restarts.

To use: 
download all files in a folder
use "npm install" for installing all dependencies
use "node server.js" to start server
Site is ready at 127.0.0.1:3000
